# Docker Compose / Cloudflare URL
# Copy to .env and set when using Cloudflare or custom domain.

# Comma-separated origins allowed by CORS (e.g. your Cloudflare public URL).
# Backend will accept requests from these origins.
CORS_ALLOWED_ORIGINS=https://your-app.trycloudflare.com,https://fast.yourdomain.com

# Optional: API base URL for the frontend (e.g. when API is on a different Cloudflare subdomain).
# Leave empty when frontend and API are same origin (e.g. nginx proxy in Docker).
# Example: https://api.yourdomain.com/api/v1
VITE_API_BASE_URL=
