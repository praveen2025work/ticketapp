# Docker Compose / Backend / Cloudflare
# Copy to .env and set values. Do not commit .env (it may contain secrets).

# ---- Backend (fast-backend) ----
# Required in production: JWT signing key (min 256 bits / 32 chars). Use a strong random secret.
APP_JWT_SECRET=

# Comma-separated origins allowed by CORS (e.g. your frontend or Cloudflare URL).
CORS_ALLOWED_ORIGINS=https://your-app.trycloudflare.com,https://fast.yourdomain.com

# Oracle (when using dev/prod profiles)
# ORACLE_HOST= ORACLE_PORT=1521 ORACLE_SID=ORCL ORACLE_USER= ORACLE_PASSWORD=

# ---- Frontend (fast-frontend) ----
# Optional: API base URL for the frontend. Leave empty when same origin.
# VITE_API_BASE_URL=
