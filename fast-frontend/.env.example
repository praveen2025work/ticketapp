# Backend API base URL (used at build time).
# Leave empty or omit to use relative /api/v1 (same origin as the frontend).
# Examples:
#   Same origin (IIS proxy): leave empty
#   Different origin:       https://api.yourserver.com/api/v1
#   Local backend:          http://localhost:8081/api/v1
VITE_API_BASE_URL=

# Auth mode when deployed (optional). Set to "local" when backend uses app.auth.mode=local
# (e.g. Windows Server with NSSM, no BAM). Frontend will call /users/me with X-Authenticated-User instead of BAM.
# VITE_AUTH_MODE=local
